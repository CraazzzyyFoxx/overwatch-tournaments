PORT=8000

# Workspace settings pickers (Discord roles/channels/server card) ask
# discord-service first and only fall back to Discord's REST API themselves when
# it is unreachable. DISCORD_TOKEN is the SAME bot token discord-service uses;
# leaving it unset only disables that fallback.
DISCORD_TOKEN=your_discord_bot_token
