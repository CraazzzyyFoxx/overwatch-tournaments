"""Gamemode service.

Generic CRUD-style operations live on `shared.repository.GamemodeRepository`
(see `flows.py`). This module is reserved for app-service-specific gamemode
queries, if any are ever needed.
"""
