{
	email {$ACME_EMAIL}
}

# Replica S3 API. Caddy forwards Host as received — required: SigV4 covers Host,
# and rustfs verifies the signature against the Host it got.
# Do not expose the console (:9001): neither replication nor mc needs it.
{$HOME_S3_DOMAIN} {
	reverse_proxy rustfs:9000
}
