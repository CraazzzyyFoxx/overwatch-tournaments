# Dev-only override, mounted by the root docker-compose.yml (monitoring profile).
# RabbitMQ restricts the default `guest` user to loopback connections, which
# blocks the rabbitmq-exporter container from reaching the management API when
# the dev broker runs with guest/guest. Production uses non-guest credentials
# and does NOT mount this file.
loopback_users.guest = false
