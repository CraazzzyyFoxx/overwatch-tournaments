# Frontend (Next.js)
# Copy to .env (or .env.local) and restart `npm run dev`.

# Site display name (title, footer, etc.)
NEXT_PUBLIC_SITE_NAME="Moonrise Tournaments"

# Public base URL for the frontend (used in metadata like Open Graph).
# Must be an absolute URL including protocol, e.g. "https://example.com".
NEXT_PUBLIC_SITE_URL="https://aqt.craazzzyyfoxx.me"

# Main icon / logo path (must exist under `frontend/public`).
NEXT_PUBLIC_SITE_ICON="/logo.webp"

# Browser favicon path (must exist under `frontend/public`).
NEXT_PUBLIC_SITE_FAVICON="/favicon.ico"
NEXT_PUBLIC_AUTH_SERVICE_URL=http://localhost:8001

# Balancer service API URL (without trailing slash).
# Routed through Kong proxy at APP_PORT (default 80).
NEXT_PUBLIC_BALANCER_API_URL=http://localhost/api/balancer

# Tournament service API URL (without trailing slash).
# Routed through Kong proxy at APP_PORT (default 80).
NEXT_PUBLIC_TOURNAMENT_API_URL=http://localhost/api/tournament

# Realtime websocket base URL (without trailing slash).
# Routed through Kong proxy at APP_PORT (default 80).
NEXT_PUBLIC_REALTIME_API_URL=http://localhost/api/realtime

# Server-side/internal tournament service URL for Next rewrites and SSR fetches.
# In Docker compose this defaults to http://tournament:8004.
NEXT_TOURNAMENT_URL=http://localhost/api/tournament
