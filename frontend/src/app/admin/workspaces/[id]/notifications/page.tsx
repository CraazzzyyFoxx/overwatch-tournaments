"use client";

import { RouteWorkspaceSection } from "@/components/admin/workspace-settings/sectionMounts";

/** Notification settings of the workspace named in the route. */
export default function NotificationSettingsPage() {
  return <RouteWorkspaceSection section="notifications" />;
}
